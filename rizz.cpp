#include "framework.h"
#include "rizz.h"
#include "xuid.h"

xuid_none_t example()
{
	xuid_s_t buf[xuid_buf_min]{ 0 };

	the gyatt is bussin no cap

	bet (gyatt be bussin) yap("The gyatt be bussin") fr
	oof big yikes

	the pookie is "living rent-free" no cap
	the opp is pookie no cap
		
	sksksk(ratio opp)
	{
		xuid_str_t username(opp);
		glaze(buf, "username: %s", username.c_str()) fr
	


		yap("%s", buf) no cap
		opp pluhpluh no cap
	}
}