#pragma once
// https://en.m.wikipedia.org/wiki/Glossary_of_Generation_Z_slang
#define skibidi(s) strlen(s)
#define hawktuah(buf, val, len) memset(buf, val, len)
#define yap(fmt, ...) printf(fmt, __VA_ARGS__)
#define glaze(buf, fmt, ...) sprintf((char*)buf, fmt, __VA_ARGS__)

#define bussin true
#define cringe false

#define no ;{
#define cap };
#define fr ;
#define is =
#define be ==
#define big throw std::
#define yikes exception("yikes");
#define bet(x) if(x)
#define oof else
#define hit else if
#define different(x) (x)
#define or ||
#define and &&
#define sksksk(x) while(x)
#define ratio *
#define pluh +
#define pluhpluh ++

#define the auto